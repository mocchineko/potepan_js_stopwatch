$(document).ready(function () {
    let timer = 0;
    let milliSeconds = 0;
    let seconds = 0;
    let minutes = 0;
    let hours = 0;

    $("milliSeconds").html(milliSeconds);
    $("seconds").html(seconds);
    $("minutes").html(minutes);
    $("hours").html(hours);

    $()
})